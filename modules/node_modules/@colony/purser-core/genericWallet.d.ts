import { GenericClassArgumentsType } from './types';
export default class GenericWallet {
    address: string;
    otherAddresses: Object[];
    chainId: number;
    type: string;
    subtype: string;
    constructor({ publicKey, chainCode, rootDerivationPath, addressCount, chainId, }: GenericClassArgumentsType);
    get publicKey(): Promise<string>;
    get derivationPath(): Promise<string>;
}
