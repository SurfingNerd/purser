import * as helpers from './helpers';
import * as validators from './validators';
import * as normalizers from './normalizers';
import * as defaults from './defaults';
import * as messages from './messages';
import * as types from './types';
import * as utils from './utils';
import GenericWallet from './genericWallet';
declare const coreModule: {
    helpers: typeof helpers;
    defaults: typeof defaults;
    validators: typeof validators;
    normalizers: typeof normalizers;
    messages: typeof messages;
    types: typeof types;
    utils: typeof utils;
    GenericWallet: typeof GenericWallet;
};
export { helpers, defaults, validators, normalizers, messages, types, utils, GenericWallet, };
export default coreModule;
